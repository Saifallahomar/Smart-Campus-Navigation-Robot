# Smart Campus Navigation Robot - Python dependencies
#
# Install inside your virtual environment on the Raspberry Pi:
#     pip install -r requirements.txt
#
# NOTE: picamera2 is NOT installed with pip on Raspberry Pi OS. It ships with
# the system Python. Create your venv with access to system packages:
#     python -m venv venv --system-site-packages
# That way picamera2 (and libcamera) are available inside the venv.

openai>=1.0.0
python-dotenv>=1.0.0
sounddevice>=0.4.6
scipy>=1.10.0
numpy>=1.24.0
pygame>=2.5.0
opencv-python>=4.8.0

# Servo head tracking (single pan servo on a GPIO pin).
# gpiozero and its pin backend (lgpio) ship PREINSTALLED on Raspberry Pi OS, so
# with a --system-site-packages venv you don't need to install anything and the
# servo works out of the box. If servo support is missing, the robot still runs
# (it just won't move the head). Listed here for reference / non-Pi-OS setups:
# gpiozero>=2.0
# lgpio>=0.2.2.0

# Optional - only needed if you turn on the Porcupine wake word engine.
# Uncomment to install:
# pvporcupine>=3.0.0
