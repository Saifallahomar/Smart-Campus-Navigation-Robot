{
  "_comment": "User-editable settings. Edit values here — no need to touch Python code. Secrets go in .env, NOT here.",

  "mock_mode": false,

  "language": "auto",

  "devices": {
    "mic_device": 1,
    "speaker_device": "plughw:2,0",
    "mic_auto_detect_name": "EMEET",
    "speaker_auto_detect": false
  },

  "audio": {
    "sample_rate": 16000,
    "chunk_seconds": 0.2,
    "voice_threshold": 500,
    "silence_to_stop": 0.9,
    "max_record_seconds": 10
  },

  "vision": {
    "enabled": true,
    "frame_width": 640,
    "frame_height": 480,
    "cascade_path": "/usr/share/opencv4/haarcascades/haarcascade_frontalface_default.xml",
    "scale_factor": 1.2,
    "min_neighbors": 5,
    "min_face_size": 60,
    "show_preview": true,
    "track_face": true,
    "feed_fps": 15
  },

  "ai": {
    "chat_model": "gpt-4o-mini",
    "transcribe_model": "gpt-4o-mini-transcribe",
    "tts_model": "gpt-4o-mini-tts",
    "tts_voice": "alloy",
    "max_history_turns": 12,
    "use_local_faq": false,
    "request_timeout": 30,
    "max_retries": 2
  },

  "wake_word": {
    "enabled": false,
    "engine": "energy",
    "keywords": ["hey robot", "hello robot"],
    "porcupine_keyword_paths": []
  },

  "ui": {
    "show_captions": true,
    "fullscreen": true,
    "fps": 30,
    "shutdown_action": "quit",

    "_layout_comment": "Portrait mode for the Pi 7-inch screen in vertical orientation. Set portrait to false for landscape (small thumbnail instead of large preview).",
    "portrait": true,
    "preview_height_frac": 0.45,
    "face_height_frac": 0.29
  },

  "conversation": {
    "_comment": "How long the robot stays in conversation mode after the person stops talking or leaves.",
    "person_lost_timeout": 8,
    "_max_session_comment": "Set max_session_seconds to a number (e.g. 300) to limit session length, or leave null for unlimited.",
    "max_session_seconds": null,
    "farewell_message": "Bye! Have a great day!"
  },

  "wave_detection": {
    "_comment": "Experimental wave-gesture detection. Set enabled to true to try it. The robot will greet someone who waves before they speak.",
    "enabled": false,
    "_threshold_comment": "motion_threshold: 0-255, higher = less sensitive. min_motion_frames: how many frames of motion before confirming a wave.",
    "motion_threshold": 30,
    "min_motion_frames": 8,
    "cooldown_seconds": 3.0
  },

  "servo": {
    "_comment": "Single pan servo that turns the head to follow a face. Signal -> GPIO 18 (physical pin 12), power -> 5V, ground -> GND. Set enabled to false if no servo is connected.",
    "enabled": true,
    "gpio_pin": 18,
    "_angle_comment": "Safe sweep range, in degrees. Narrow these if the head hits something.",
    "min_angle": -90,
    "max_angle": 90,
    "center_angle": 0,
    "_motion_comment": "max_speed_deg_per_sec limits how fast the head turns (smaller = gentler). deadzone_deg ignores tiny offsets so it doesn't jitter. idle_release_seconds: how long to wait, once settled, before the servo goes quiet.",
    "max_speed_deg_per_sec": 120,
    "deadzone_deg": 1.0,
    "idle_release_seconds": 1.5,
    "_mount_comment": "Set invert to true if the head turns the wrong way. Pulse widths suit a typical SG90; adjust if your servo's range is off.",
    "invert": false,
    "min_pulse_width_ms": 0.5,
    "max_pulse_width_ms": 2.5
  }
}
