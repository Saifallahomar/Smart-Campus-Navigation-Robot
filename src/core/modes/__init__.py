"""
Robot operating modes (future features).

Each module here is a lightweight PLACEHOLDER for a bigger feature that is not
wired into the main loop yet. They exist so the structure is ready and the plan
is documented in code — importing them does nothing and changes no behaviour.

Planned modes:
  - tour_mode.py        Guided campus tour (walk-and-talk through key buildings).
  - navigation_mode.py  Turn-by-turn directions to a destination on campus.

When you are ready to build one, fill in its `run()` method and call it from
src/core/robot.py based on what the user asks for.
"""
